package nu.placebo.whatsup.network;

/**
 * 
 * Used when OperationResults need to specify more information abnout the task
 * the performed.
 * 
 */

public enum Action {
	DEFAULT, LOG_IN, LOG_OUT, TEST_SESSION, CREATE, RETRIEVE, UPDATE
}
